pipeline {
    agent none
    stages {
        stage('Test') {
            steps {
                sh '''
                  ls -la
                  cat Dockerfile
                '''
            }
        }
    }
}
